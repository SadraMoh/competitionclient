import { TelnoDirective } from './telno/telno.directive';

describe('TelnoDirective', () => {
  it('should create an instance', () => {
    const directive = new TelnoDirective();
    expect(directive).toBeTruthy();
  });
});
